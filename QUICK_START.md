# دليل البدء السريع - Quick Start Guide

## الخطوات المطلوبة بالترتيب:

### ✅ الخطوة 1: تثبيت المكتبات
افتح Terminal في مجلد `backend` ونفذ:
```bash
cd backend
npm install
```

### ✅ الخطوة 2: إعداد قاعدة البيانات MySQL

#### أ) إذا كان MySQL مثبت على جهازك:
1. افتح MySQL Command Line أو MySQL Workbench
2. سجل دخول:
   ```bash
   mysql -u root -p
   ```
   (أدخل كلمة المرور)

3. نفذ ملف `schema.sql`:
   ```sql
   source D:/smc-digital-suite/backend/schema.sql
   ```
   أو انسخ محتوى `schema.sql` والصقه في MySQL

#### ب) إذا لم يكن MySQL مثبت:
- حمّل من: https://dev.mysql.com/downloads/installer/
- أو استخدم XAMPP (يحتوي على MySQL)

### ✅ الخطوة 3: إنشاء ملف `.env`

في مجلد `backend`، أنشئ ملف اسمه `.env` (بدون أي امتداد) واكتب فيه:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=smc_dashboard
PORT=3000
```

**مهم**: استبدل `your_mysql_password` بكلمة مرور MySQL الخاصة بك

### ✅ الخطوة 4: تشغيل السيرفر

```bash
cd backend
npm start
```

إذا ظهرت رسالة:
```
✅ MySQL Database connected successfully!
🚀 Server running on http://localhost:3000
```

**يعني كل شيء تمام! ✅**

---

## للاتصال بقاعدة بيانات بعيدة (Remote MySQL):

إذا كان لديك MySQL على سيرفر آخر، غيّر في ملف `.env`:

```env
DB_HOST=your-mysql-host.com
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=smc_dashboard
PORT=3000
```

---

## استكشاف الأخطاء:

### ❌ خطأ: "Access denied"
- تأكد من صحة username و password في `.env`
- تأكد من أن المستخدم لديه صلاحيات على قاعدة البيانات

### ❌ خطأ: "Can't connect to MySQL server"
- تأكد من أن MySQL service يعمل
- في Windows: اذهب إلى Services وابدأ MySQL
- تأكد من صحة `DB_HOST` في `.env`

### ❌ خطأ: "Unknown database 'smc_dashboard'"
- تأكد من إنشاء قاعدة البيانات أولاً (نفذ `schema.sql`)

---

## بعد نجاح الاتصال:

1. السيرفر سيعمل على: `http://localhost:3000`
2. يمكنك اختبار API من المتصفح: `http://localhost:3000/api/products`
3. البيانات ستُحفظ في MySQL بدلاً من localStorage
4. جميع التعديلات ستظهر على الموقع مباشرة

---

## ملاحظات:

- ✅ البيانات محفوظة بشكل دائم في MySQL
- ✅ البيانات مشتركة بين جميع المستخدمين
- ✅ يمكنك عمل Backup بسهولة من MySQL
- ✅ يمكنك إدارة البيانات من MySQL Workbench



