# كيفية إضافة البيانات (Seeds) إلى الخادم البعيد

## المشكلة
الأمر `npm run seed` يعمل بنجاح محلياً لكن البيانات لا تظهر على الخادم البعيد (Railway).

## الحل

### 1. تأكد من ملف `.env` يحتوي على معلومات الخادم البعيد

افتح ملف `.env` وتأكد من وجود:

```env
# معلومات قاعدة البيانات البعيدة (Railway)
DB_HOST=switchback.proxy.rlwy.net
DB_PORT=28406
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=railway

# أو استخدم DATABASE_URL مباشرة
DATABASE_URL=mysql://root:password@switchback.proxy.rlwy.net:28406/railway
```

### 2. تشغيل Seed مع التأكد من الاتصال

```powershell
# تأكد من الاتصال بقاعدة البيانات الصحيحة
npm run seed
```

### 3. التحقق من البيانات

بعد التشغيل، ستظهر رسالة في النهاية:
```
🔍 Verifying data was inserted...
   Users in database: 4
   Categories in database: 5
   Products in database: 5
```

### 4. إذا كانت الأرقام = 0

هذا يعني أن البيانات لم تُضف. جرب:

```powershell
# تحقق من الاتصال بقاعدة البيانات
npm run check-db

# أو
npx prisma studio
```

### 5. تشغيل Seed على الخادم مباشرة

إذا كان لديك SSH access إلى الخادم:

```bash
# على الخادم
cd /path/to/backend
npm run seed
```

### 6. استخدام Prisma Studio للتحقق

```powershell
npx prisma studio
```

سيفتح متصفح على `http://localhost:5555` يمكنك من رؤية البيانات.

## نصائح إضافية

### تأكد من أن Seed يمسح البيانات القديمة

الـ seed script يقوم بـ `deleteMany()` أولاً، لذا تأكد من أنك تتصل بقاعدة البيانات الصحيحة.

### إضافة Seed إلى CI/CD

إذا كنت تستخدم Railway أو Vercel، يمكنك إضافة seed script إلى workflow:

```yaml
- name: Seed database
  run: npm run seed
  env:
    DATABASE_URL: ${{ secrets.DATABASE_URL }}
```

## استكشاف الأخطاء

### خطأ: "Cannot connect to database"
- تحقق من معلومات الاتصال في `.env`
- تأكد من أن IP الخاص بك مسموح به في Railway
- تحقق من كلمة المرور

### خطأ: "Table doesn't exist"
```powershell
npx prisma migrate deploy
```

### البيانات تظهر محلياً فقط
- تأكد من أن `.env` يحتوي على معلومات الخادم البعيد
- لا تستخدم `.env.local` للخادم البعيد

